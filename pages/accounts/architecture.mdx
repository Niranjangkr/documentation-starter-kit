import { MermaidDisplay } from '../../components/MermaidDisplay'

# Architecture

This page describes how Agile Receivables models customers, accounts, campaigns, payments, and disputes,
and how external systems integrate with the API.

## High-level system diagram

<MermaidDisplay
  chart={`
flowchart TD
    ERP["ERP Systems / Manual Upload (CSV, API, Webhooks)"]
    API["Agile Receivables API"]
    Customers["Customers (encrypted PII)"]
    Invoices["Invoices / Accounts (encrypted fields)"]
    Campaigns["Campaigns (workflow definitions)"]
    Exec["Campaign Execution Engine (state machine per invoice)"]
    Email["Email Node -> SES / SendGrid"]
    SMS["SMS Node -> Twilio"]
    Voice["Voice Node -> Retell / Twilio"]
    WaitNode["Wait / Segment / Branch Nodes"]
    Comms["Communications (sent message records)"]
    PayLink["Payment Link Generation (Stripe)"]
    WHStripe["Stripe Connect Webhooks"]
    WHTwilio["Twilio Webhooks"]
    WHRetell["Retell Webhooks"]
    InvoiceStatus["Invoice Status Update (SETTLED on full payment)"]
    Audit["Audit Logs"]

    ERP --> API
    API --> Customers
    API --> Invoices
    API --> Campaigns
    Campaigns --> Exec
    Exec --> Email
    Exec --> SMS
    Exec --> Voice
    Exec --> WaitNode
    Email --> Comms
    SMS --> Comms
    Voice --> Comms
    Comms --> PayLink
    PayLink --> WHStripe
    WHStripe --> InvoiceStatus
    WHTwilio --> InvoiceStatus
    WHRetell --> InvoiceStatus
    InvoiceStatus --> Exec
    Exec --> Audit
`}
/>

## Entity relationship diagram

<MermaidDisplay
  chart={`
erDiagram
    Company ||--o{ Customer : has
    Company ||--o{ Invoice : has
    Company ||--o{ Campaign : has
    Company ||--o{ ApiKey : has
    Company ||--o{ User : members

    Customer ||--o{ Invoice : has
    Customer ||--o{ Communication : receives
    Customer ||--o{ Grievance : raises

    Invoice }o--|| Campaign : assigned_to
    Invoice ||--o{ Communication : linked_to
    Invoice ||--o{ Payment : has
    Invoice ||--o{ Grievance : has
    Invoice ||--o| CampaignExecution : active_execution

    Campaign ||--o{ CampaignExecution : runs
    CampaignExecution }o--|| Invoice : for_invoice
    CampaignExecution }o--|| Customer : for_customer

    Grievance ||--o{ GrievanceMessage : has
    Grievance }o--|| User : assigned_to
    GrievanceMessage }o--o| User : sent_by
`}
/>

## Customer field validations

| Field | Required | Rules |
| --- | --- | --- |
| `first_name`, `last_name` | No | Encrypted at rest |
| `email` | No | Valid email format if provided; encrypted |
| `phone` | No | Encrypted |
| `company_name` | No | String |
| `external_id` | No | String (for ERP linking) |
| `tags` | No | Array of strings |
| `timezone` | No | String |
| `communication_preferences` | No | Object: `{email, sms, voice, do_not_disturb}` booleans |
| `custom_fields` | No | JSONB dict |

## Invoice (Account) field validations

| Field | Required | Rules |
| --- | --- | --- |
| `invoice_number` | Yes | String, indexed |
| `customer_id` | Yes | UUID |
| `amount` | Yes | Decimal(10,2) |
| `due_date` | Yes | Date |
| `issue_date` | Yes | Date |
| `currency` | No | Default: `USD` |
| `paid_amount` | No | Default: 0.00 |
| `status` | No | Default: `not_assigned` |
| `source` | No | Enum: `api`, `sheets`, `in_app` |
| `payment_terms` | No | Int, default: 30 (days) |
| `description`, `line_items`, `notes` | No | Encrypted |
| `custom_fields` | No | JSONB |
| `is_written_off` | No | Boolean, default: false |

## Invoice status lifecycle

<MermaidDisplay
  chart={`
stateDiagram-v2
    [*] --> NOT_ASSIGNED : Invoice created
    NOT_ASSIGNED --> IN_PROGRESS : Enrolled in campaign
    IN_PROGRESS --> PENDING_COLLECTION : Campaign completes (no next campaign)
    IN_PROGRESS --> DISPUTED : Dispute raised
    IN_PROGRESS --> SETTLED : Full payment received
    IN_PROGRESS --> UNCOLLECTIBLE : Manual write-off
    IN_PROGRESS --> SUSPENDED : Manually suspended
    PENDING_COLLECTION --> IN_PROGRESS : Enrolled in new campaign
    PENDING_COLLECTION --> UNCOLLECTIBLE : Written off
    DISPUTED --> IN_PROGRESS : All disputes resolved (has campaign)
    DISPUTED --> NOT_ASSIGNED : All disputes resolved (no campaign)
    DISPUTED --> UNCOLLECTIBLE : Written off
    DISPUTED --> SUSPENDED : Manually suspended
    SUSPENDED --> IN_PROGRESS : Manually unsuspended
    SETTLED --> [*] : Locked
    UNCOLLECTIBLE --> [*] : Locked
`}
/>

## Multi-tenant and workspace model

<MermaidDisplay
  chart={`
flowchart TD
    Org["Organization (Company with is_org=true)"]
    Co1["Company / Workspace 1"]
    Co2["Company / Workspace 2"]
    Key1["API Key A (scoped to Company 1)"]
    Key2["API Key B (scoped to Company 2)"]
    User["User"]
    Mem1["Membership (role: owner)"]
    Mem2["Membership (role: member)"]

    Org --> Co1
    Org --> Co2
    Co1 --> Key1
    Co2 --> Key2
    User --> Mem1 --> Co1
    User --> Mem2 --> Co2
`}
/>

- `Company` = workspace.
- API keys are permanently bound to one company/workspace.
- Users can be members of multiple companies with different roles.
- All data queries are implicitly filtered by `company_id`.
- Scopes are per-key, not per-tenant.

